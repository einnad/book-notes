<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/style.css" />
    <title>yourwords</title>
  </head>
  <body>
    <header>
      <img src="/img/logo.png" alt="yourwords logo" />
      <a href="/search">Search books</a>
      <a href="/post">Post a review</a>
    </header>

    <!-- <form class="login-form" action="/login" method="post">
      <p>Name:</p>
      <input type="text" name="reader-name" />
      <p>Pin:</p>
      <input type="text" name="reader-pin" />
      <input type="submit" />
    </form> -->
    <h1>Book reviews</h1>

    <h2>Reviews</h2>
    <div class="wrapper">
      <% if (locals.reviews) { %> <% reviews.forEach(rev => { %>
      <div class="review-box">
        <p><%=rev.title%> written by <%=rev.author%></p>
        <p>read by</p>
        <p><%=rev.review%></p>
        <form action="/delete" method="post">
          <input
            type="submit"
            name="deleteId"
            value="Delete review <%= rev.id %>"
          />
        </form>
      </div>
      <% }); %> <% } %>
    </div>

    <footer>© Dannielle 2024</footer>
  </body>
</html>
